# Web runtime configuration
# Use comma-separated origins if you need browser clients from multiple domains.
CORS_ORIGIN=http://localhost:5173

# Optional runtime controls
NODE_ENV=production
PORT=8787
OPEN_BROWSER=0
